/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: [
        'knhomhsepebhoyruqkfi.supabase.co', // ✅ your Supabase image host
      ],
    },
  };

module.exports = nextConfig
